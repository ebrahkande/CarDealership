import com.pluralsight.UserInterface;

public class Program {
    public static void main(String[] args) {
        // Main method to the entry point of the entire application
        UserInterface app = new UserInterface();
        // Call the display() method to start the application's menu loop
        app.display();
    }
}
